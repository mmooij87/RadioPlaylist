name: Kink Radio Playlist Scraper

on:
  schedule:
    - cron: '*/1 * * * *'  # Run every 10 minutes
  workflow_dispatch:  # Allow manual triggering

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    timeout-minutes: 5  # Set timeout to 5 minutes
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4
          
      - name: Run playlist scraper
        run: python scrape_playlist.py
        
      - name: Commit and push if changed
        run: |
          git config --global user.name 'GitHub Actions'
          git config --global user.email 'github-actions@github.com'
          git add docs/playlist.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update playlist data" && git push)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}